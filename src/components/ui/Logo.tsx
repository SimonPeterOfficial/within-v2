import WithinMark from "@/components/ui/WithinMark";

/** The WithIn wordmark — now carried by the mark system (aperture + light). */
export default function Logo() {
  return <WithinMark variant="wordmark" size={26} />;
}
